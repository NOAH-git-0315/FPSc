import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#143046',
    },
    secondary: {
      main: '#44bca1',
    },
    background: {
      paper: '#fdfdfd',
    },
  },
});

export default theme;
