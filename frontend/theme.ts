import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#143046',
    },
    secondary: {
      main: '#44bca1',
    },
    background: {
      paper: '#fdfdfd',
    },
  },
});

export default theme;

// pageTopMargin: 32,
// sectionMargin: 24,
// itemMargin: 16,
