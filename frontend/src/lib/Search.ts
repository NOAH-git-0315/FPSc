import { gameRanks } from './Array/Rank';
import { Game } from '@/app/type';
interface fetchFriendType {
    game: Game[]
    rank:gameRanks[]
    playstyle:
    playtime:
}

export const fetchFriend = async ({game, rank, playstyle, playtime}:) => {
    if game.includes
};
